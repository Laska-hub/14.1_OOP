# 14.1_OOP — Управление категориями и товарами

Проект демонстрирует работу с объектно-ориентированным программированием в Python.  
Реализованы классы `Product` и `Category`, поддержка загрузки данных из JSON и тестирование через `pytest`.

## 📁 Структура проекта

14.1_OOP/
│
├── products.json # Файл с данными товаров и категорий
├── src/
│ └── main.py # Основной код проекта
├── tests/
│ └── test_main.py # Тесты с pytest
├── pyproject.toml # Конфигурация Poetry и зависимостей
├── poetry.lock # Зафиксированные версии пакетов
└── README.md # Этот файл

## ⚙️ Установка и настройка

1. Клонируем проект:

```bash
git clone <URL_репозитория>
cd 14.1_OOP

Устанавливаем зависимости и создаём виртуальное окружение через Poetry:

poetry install
poetry shell

Убедитесь, что Python 3.13 активен:

python --version

▶Запуск проекта

Внутри виртуального окружения:

python src/main.py

Запуск тестов
poetry run pytest
======= 4 passed in 0.01s ====

## 14.2 – Инкапсуляция ветка: 14.2-incapsulation

На данном этапе были реализованы следующие улучшения:

- Цена товара (`Product.price`) сделана приватной, добавлены геттер и сеттер
- Список товаров в классе `Category` сделан приватным
- Добавлен метод `add_product()` для безопасного добавления товара в категорию
- Реализован `@property` для форматированного вывода списка товаров
- Добавлен класс-метод `Product.new_product()` для создания товара из словаря
- Реализована проверка корректности цены (запрет на нулевую и отрицательную цену)
- Обновлены и добавлены unit-тесты для новой функциональности: ======= 3 passed in 0.01s ====


15.1 Магические методы: ветка 15.1-magic-methods

## 🚀 Реализованный функционал

### 🔹 Класс `Product`
- Приватный атрибут цены (`__price`)
- Геттер и сеттер для price с валидацией
- Строковое отображение (`__str__`)
- Сложение товаров (`__add__`)
- Поддержка типизации
- Тесты: ===== 4 passed in 0.01s ===

16.1 Наследование

- Наследование
- Ограничение сложения через type()
- Проверка типов через isinstance()
- Защита API класса
- Поддержка типизации
- Полное тестовое покрытие новой функциональности ===== 9 passed in 0.01s ===

16.2 – Абстрактные классы и миксины

- В проект добавлен абстрактный класс `BaseProduct`, определяющий общую функциональность для всех продуктов.
- Добавлен класс-миксин `InitReprMixin`, который выводит информацию о создании объекта.
- Классы `Product`, `Smartphone` и `LawnGrass` реализуют абстрактные методы.
- Добавлены тесты для новой функциональности: ===== 8 passed in 0.04s ====
- Поддержка типизации

17.1 – Обработка исключений
ветка: 17.1-exceptions
На данном этапе была реализована обработка ошибок и улучшена устойчивость работы программы.
Добавленный функционал
При создании объекта Product теперь выбрасывается ValueError, если количество товара равно 0.
Добавлена защита сеттера price — запрещена установка отрицательной или нулевой цены.
В классе Category реализован метод middle_price() для вычисления средней цены всех товаров категории.
Добавлена обработка ситуации пустой категории (возвращается 0 вместо ошибки деления).
Реализована проверка типов при сложении товаров.
Реализована проверка типов при добавлении товара в категорию.

Пример обработки ошибок
try:
    product = Product("Test", "Example", 100, 0)
except ValueError as e:
    print(e)
Тестирование

Добавлены тесты для проверки:
- создания товара с нулевым количеством
- работы сеттера price
- метода middle_price()
- сложения товаров
- добавления товаров в категорию
- обработки ошибок
- Результат запуска тестов: ===== 13 passed in 0.02s ===
- Поддержка типизации